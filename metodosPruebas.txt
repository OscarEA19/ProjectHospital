void deleteUser(listaUser &cab,int userCode){  listaUser aux,ant;   if(cab == NULL){      cout <<"Lista vacia";  }  else{   aux =cab;   if(aux -> userCode == userCode){    cab = aux -> next;    aux -> next = NULL;    delete(aux);    aux =cab;   }   else{    ant =aux;    while(aux!=NULL){rCode == userCod     if(aux->usee && aux -> next !=NULL){      ant->next=aux->next;      aux->next=NULL;      delete(aux);      aux=cab;     }     else{      if(aux->userCode==userCode && aux->next==NULL){       ant->next=NULL;       delete(aux);       aux=cab;      }     }     ant = aux;     aux = aux ->next;    }   }  } }
